pip3 install numpy
# We need gfortran to compile SciPy (deprecated for pip3 scipy)
# brew install gfortran --cask
pip3 install scipy
# To install matplotlib, we need to install pkg-config
brew install pkg-config
pip3 install matplotlib
pip3 install pandas
brew install opencv
pip3 install tensorflow
pip3 install keras
